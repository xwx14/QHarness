# 工具分发 Fork-Join 重构实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 `Engine2StageReAct`/`EngineReActLoop` 的串行工具分发改为 Fork-Join 并发（资源键分桶串行化保证同文件正确性），并发度可配（Settings，`0` = 无上限）。

**Architecture:** `ToolManager::executeAll` 按各工具声明的 `resourceKey` 分桶（同键桶内按原序串行、异键桶间并发），`std::async` 实现，异常隔离；`Tool` 基类加 `resourceKey` 虚方法，文件类/bash override；`Engine` 基类 setter 注入并发度；两引擎共用 `executeAll`。

**Tech Stack:** C++17、`std::async`/`future`、`std::atomic`、nlohmann/json、Qt5 Widgets（SettingsDialog）、项目自研 TestHarness（`QH_TEST`/`QH_CHECK`）。

**Design spec:** `docs/specs/2026-07-01-工具分发Fork-Join重构设计.md`

## Global Constraints

- C++17、MSVC、`/utf-8`（中文注释/字符串正常）。
- 头文件 include guard 命名 `QH_<模块>_<文件>_H`；不用 `#pragma once`。
- 类成员变量下划线前缀（`_maxToolConcurrency`）。
- 所有头文件 `.h` 须列入 `core/CMakeLists.txt` 源列表（VS 可见）。
- 注释、commit message 用中文；变量/函数驼峰。
- Settings 序列化用 ADL `to_json`/`from_json`（`from_json` 用 `j.value()` 缺字段容错）。
- 回归测试用 `QH_TEST` 写在 `core/tests/test_*.cpp` 并在 `core/CMakeLists.txt` 的 `qharness_core_tests` 源列表登记；提交前 `./run_tests.bat` 须 `0 failures`。
- `Tool` 基类 `resourceKey` 默认返回 `std::nullopt`（不破坏现有/未来不操作共享资源的工具）。
- 工具 `execute` 必须无可变实例状态（纯函数式），方能安全并发。

---

## File Structure

| 文件 | 责任 | 改动 |
|---|---|---|
| `core/schema/Settings.{h,cpp}` | 应用配置 | 加 `_maxToolConcurrency` + 序列化 |
| `core/tool/Tool.h` | 工具基类 | 加 `resourceKey` 虚方法 |
| `core/tool/{ReadFile,WriteFile,EditFile}Tool.h` | 文件类工具 | override `resourceKey`（规范路径） |
| `core/tool/{Win,Linux,Mac}BashTool.h` | Bash 系列 | override `resourceKey`（`__bash__`） |
| `core/tool/ToolManager.{h,cpp}` | 工具注册/分发 | 加 `executeAll`（分桶 Fork-Join） |
| `core/engine/Engine.h` | 引擎基类 | 加 `_maxToolConcurrency` + setter |
| `core/engine/{Engine2StageReAct,EngineReActLoop}.cpp` | 引擎实现 | 分发循环换 `executeAll` |
| `app/EngineThread.cpp` | 引擎线程装配 | 注入并发度 |
| `app/SettingsDialog.{h,cpp}` | 设置对话框 | 加并发度 QSpinBox |
| `core/tests/test_Settings.cpp` | Settings 测试 | 加并发度序列化用例 |
| `core/tests/test_ToolManager.cpp` | ToolManager 测试 | 加探针工具 + `executeAll` 用例 |

---

## Task 1: Settings 并发度字段

**Files:**
- Modify: `core/schema/Settings.h:56`（`_enabledTools` 后加字段）
- Modify: `core/schema/Settings.cpp:48-65`（`to_json`/`from_json`）
- Test: `core/tests/test_Settings.cpp`

**Interfaces:**
- Produces: `qh::schema::Settings::_maxToolConcurrency`（`int`，默认 `0`）；JSON 键 `"maxToolConcurrency"`；`from_json` 缺字段容错为 `0`。

- [ ] **Step 1: 写失败测试**（追加到 `test_Settings.cpp` 末尾）

```cpp
QH_TEST(settings_max_concurrency_default_zero) {
    qh::schema::Settings s;
    QH_CHECK_EQ(s._maxToolConcurrency, 0);
}

QH_TEST(settings_max_concurrency_roundtrip) {
    qh::schema::Settings s;
    s._maxToolConcurrency = 4;
    json j = s;
    QH_CHECK_EQ((int)j["maxToolConcurrency"], 4);
    auto back = j.get<qh::schema::Settings>();
    QH_CHECK_EQ(back._maxToolConcurrency, 4);
}

QH_TEST(settings_max_concurrency_omitempty_and_default) {
    qh::schema::Settings s;            // 默认 0
    json j = s;
    QH_CHECK(!j.contains("maxToolConcurrency"));   // 0 时省略
    auto back = j.get<qh::schema::Settings>();
    QH_CHECK_EQ(back._maxToolConcurrency, 0);
    // 缺字段容错
    auto j2 = json::parse(R"({"providers":[]})");
    QH_CHECK_EQ(j2.get<qh::schema::Settings>()._maxToolConcurrency, 0);
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cmake --build build --config Release --target qharness_core_tests 2>&1 | grep -i error`
Expected: 编译错误 `_maxToolConcurrency` 不是 `Settings` 成员。

- [ ] **Step 3: 实现**（`Settings.h` 第 56 行 `_enabledTools` 后加字段）

```cpp
    std::vector<std::string> _enabledTools;
    int                      _maxToolConcurrency = 0;   // 工具并发上限，0 = 无上限
```

`Settings.cpp` 的 `to_json`（第 55 行 `_enabledTools` 后加）：

```cpp
    if (!s._enabledTools.empty())       j["enabledTools"] = s._enabledTools;
    if (s._maxToolConcurrency != 0)     j["maxToolConcurrency"] = s._maxToolConcurrency;
```

`from_json`（第 64 行 `_enabledTools` 后加）：

```cpp
    if (j.contains("enabledTools"))       j.at("enabledTools").get_to(s._enabledTools);
    s._maxToolConcurrency = j.value("maxToolConcurrency", 0);
```

- [ ] **Step 4: 跑测试确认通过**

Run: `./run_tests.bat`
Expected: `[PASS] All regression tests passed.`，新增 3 用例无 FAIL。

- [ ] **Step 5: Commit**

```bash
git add core/schema/Settings.h core/schema/Settings.cpp core/tests/test_Settings.cpp
git commit -m "feat(schema): Settings 增加 _maxToolConcurrency 工具并发上限字段"
```

---

## Task 2: Tool::resourceKey 基类 + 文件类工具 override

**Files:**
- Modify: `core/tool/Tool.h`（加虚方法）
- Modify: `core/tool/ReadFileTool.h`、`core/tool/WriteFileTool.h`、`core/tool/EditFileTool.h`（各 override）
- Test: `core/tests/test_ToolManager.cpp`（追加 resourceKey 测试段）

**Interfaces:**
- Consumes: 基类 `Tool::resolveWorkBase()`、`Tool::resolveInsideForWrite(base, relPath)`、`schema::ToolCall::parseArguments()`。
- Produces: `Tool::resourceKey(call) -> std::optional<std::string>`（基类默认 `nullopt`）；文件类返回规范化工作区内绝对路径字符串。

- [ ] **Step 1: 写失败测试**（追加到 `test_ToolManager.cpp`，顶部 include 区加 `#include "tool/ReadFileTool.h"`、`#include "tool/WriteFileTool.h"`、`#include "tool/EditFileTool.h"`、`#include <filesystem>`）

```cpp
namespace fs = std::filesystem;
namespace {
fs::path probeWorkDir() {
    fs::path dir = fs::temp_directory_path() / "qh_reskey_test";
    fs::create_directories(dir);
    return dir;
}
qh::schema::ToolCall callWithPath(const std::string& path) {
    qh::schema::ToolCall c;
    c._name = "x";
    c._arguments = std::string("{\"path\":\"") + path + "\"}";
    return c;
}
} // namespace

QH_TEST(resourceKey_file_tools_same_path_same_key) {
    const fs::path dir = probeWorkDir();
    qh::tool::ReadFileTool r(dir.string());
    qh::tool::WriteFileTool w(dir.string());
    qh::tool::EditFileTool e(dir.string());
    auto kr = r.resourceKey(callWithPath("a.txt"));
    auto kw = w.resourceKey(callWithPath("a.txt"));
    auto ke = e.resourceKey(callWithPath("a.txt"));
    QH_CHECK(kr.has_value() && kw.has_value() && ke.has_value());
    QH_CHECK_EQ(*kr, *kw);    // read/write 同 path → 同 key（读写互斥）
    QH_CHECK_EQ(*kr, *ke);
}

QH_TEST(resourceKey_file_tools_diff_path_diff_key) {
    const fs::path dir = probeWorkDir();
    qh::tool::ReadFileTool r(dir.string());
    auto k1 = r.resourceKey(callWithPath("a.txt"));
    auto k2 = r.resourceKey(callWithPath("b.txt"));
    QH_CHECK(k1.has_value() && k2.has_value());
    QH_CHECK(*k1 != *k2);
}

QH_TEST(resourceKey_file_tools_missing_or_traversal_returns_nullopt) {
    const fs::path dir = probeWorkDir();
    qh::tool::ReadFileTool r(dir.string());
    qh::schema::ToolCall noPath; noPath._arguments = "{}";
    QH_CHECK(!r.resourceKey(noPath).has_value());                  // 缺 path
    QH_CHECK(!r.resourceKey(callWithPath("../../../../etc/passwd")).has_value());  // 越界
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cmake --build build --config Release --target qharness_core_tests 2>&1 | grep -i error`
Expected: `resourceKey` 不是 `Tool` 成员。

- [ ] **Step 3: 基类加虚方法**（`Tool.h`，`execute` 声明后、`protected:` 前）

```cpp
    virtual schema::ToolResult execute(const schema::ToolCall& call) = 0;

    // 该调用锁定的资源键：相同键的调用在 executeAll 中被串行化执行；
    // nullopt 表示不参与串行化（自由并发）。默认 nullopt。
    virtual std::optional<std::string> resourceKey(const schema::ToolCall& call) const {
        return std::nullopt;
    }
```

（`Tool.h` 已 `#include <optional>`，无需再加。）

- [ ] **Step 4: 文件类工具 override**（三处相同实现，各加到对应 `.h` 的 `execute` 声明后）

`ReadFileTool.h` / `WriteFileTool.h` / `EditFileTool.h` 各加：

```cpp
#include <filesystem>
#include <optional>
#include "tool/Tool.h"   // 已有，确认
```
在 `execute` 声明后加：
```cpp
    schema::ToolResult execute(const schema::ToolCall& call) override;

    // 同一文件路径 → 同一资源键（executeAll 据此把同文件操作串行化，防 lost update）
    std::optional<std::string> resourceKey(const schema::ToolCall& call) const override;
```

三处 `.cpp` 各加实现（相同）：

```cpp
std::optional<std::string> XxxTool::resourceKey(const schema::ToolCall& call) const {
    const nlohmann::json args = call.parseArguments();
    if (!args.is_object() || !args.contains("path") || !args["path"].is_string())
        return std::nullopt;
    const std::string relPath = args["path"].get<std::string>();
    const fs::path base = resolveWorkBase();
    if (base.empty()) return std::nullopt;
    auto hit = resolveInsideForWrite(base, relPath);   // 穿越检查 + 规范化，不查存在性
    return hit ? std::optional<std::string>(hit->string()) : std::nullopt;
}
```

> 各 `.cpp` 顶部确保 `#include <nlohmann/json.hpp>`、`namespace fs = std::filesystem;`（ReadFile/EditFile 已有；WriteFile 已有 json，加 `namespace fs = std::filesystem;` 若无）。

- [ ] **Step 5: 跑测试确认通过**

Run: `./run_tests.bat`
Expected: 新增 3 resourceKey 用例 PASS。

- [ ] **Step 6: Commit**

```bash
git add core/tool/Tool.h core/tool/ReadFileTool.h core/tool/ReadFileTool.cpp \
        core/tool/WriteFileTool.h core/tool/WriteFileTool.cpp \
        core/tool/EditFileTool.h core/tool/EditFileTool.cpp core/tests/test_ToolManager.cpp
git commit -m "feat(tool): Tool 基类增加 resourceKey，文件类工具按路径归桶"
```

---

## Task 3: BashTool 系列 override resourceKey

**Files:**
- Modify: `core/tool/WinBashTool.h`、`core/tool/LinuxBashTool.h`、`core/tool/MacBashTool.h`（各加 override；`__bash__` 使所有 bash 归一桶串行）
- Test: `core/tests/test_ToolManager.cpp`（WinBashTool 用例仅 Windows 编译）

**Interfaces:**
- Produces: `WinBashTool`/`LinuxBashTool`/`MacBashTool` 的 `resourceKey` 固定返回 `"__bash__"`。

- [ ] **Step 1: 写失败测试**（追加到 `test_ToolManager.cpp`，Windows 专属用 `#ifdef _WIN32` 包裹）

```cpp
#ifdef _WIN32
#include "tool/WinBashTool.h"
QH_TEST(resourceKey_winbash_returns_global_key) {
    qh::tool::WinBashTool b(".");          // resourceKey 不依赖工作目录
    qh::schema::ToolCall c; c._name = "bash"; c._arguments = R"({"command":"ls"})";
    auto k = b.resourceKey(c);
    QH_CHECK(k.has_value());
    QH_CHECK_EQ(*k, std::string("__bash__"));
}
#endif
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cmake --build build --config Release --target qharness_core_tests 2>&1 | grep -i error`
Expected: `resourceKey` 在 `WinBashTool` 未声明 override（实际走基类返回 nullopt，测试 `has_value()` FAIL）。先确认测试因返回 nullopt 而 FAIL（非编译错误）：
Run: `build/out/Release/qharness_core_tests.exe 2>&1 | grep resourceKey`
Expected: `FAIL: k.has_value()`。

- [ ] **Step 3: 三个 BashTool 头文件加 override**

`WinBashTool.h`（`execute` 声明后加）：

```cpp
    schema::ToolResult execute(const schema::ToolCall& call) override;

    // bash 命令的目标文件无法从 command 静态推断；所有 bash 归一桶串行（避免命令互相干扰）
    std::optional<std::string> resourceKey(const schema::ToolCall& call) const override {
        return "__bash__";
    }
```

`LinuxBashTool.h`、`MacBashTool.h` 加完全相同的 override（三平台一致；CMake 只编当前平台那份）。

- [ ] **Step 4: 跑测试确认通过**

Run: `./run_tests.bat`
Expected: `resourceKey_winbash_returns_global_key` PASS（Windows）。

- [ ] **Step 5: Commit**

```bash
git add core/tool/WinBashTool.h core/tool/LinuxBashTool.h core/tool/MacBashTool.h core/tests/test_ToolManager.cpp
git commit -m "feat(tool): Bash 系列工具 resourceKey 固定 __bash__ 归桶串行"
```

---

## Task 4: ToolManager::executeAll（分桶 Fork-Join 核心）

**Files:**
- Modify: `core/tool/ToolManager.h`（声明 `executeAll` + include）
- Modify: `core/tool/ToolManager.cpp`（实现）
- Test: `core/tests/test_ToolManager.cpp`（探针工具 + 7 用例）

**Interfaces:**
- Consumes: `Tool::resourceKey(call)`（Task 2/3）、`ToolManager::execute(call)`、`ToolManager::getTool(name)`。
- Produces: `ToolManager::executeAll(calls, maxConcurrency) -> std::vector<schema::ToolResult>`（与 `calls` 同序；`maxConcurrency<=0` 无上限）。

- [ ] **Step 1: 写探针工具 + 失败测试**（追加到 `test_ToolManager.cpp` 顶部 include 区加 `#include <atomic>`、`#include <chrono>`、`#include <thread>`、`#include <future>`、`#include <map>`、`#include <algorithm>`）

```cpp
// 探针工具：可配置 resourceKey、是否 sleep、是否抛异常；记录同时在跑的峰值（测并发度）
class ProbeTool : public qh::tool::Tool {
public:
    ProbeTool(std::string name, std::optional<std::string> key,
              int sleepMs = 0, bool throwOnExec = false)
        : Tool(makeDef(name)), _key(std::move(key)), _sleepMs(sleepMs), _throw(throwOnExec) {}

    std::optional<std::string> resourceKey(const qh::schema::ToolCall&) const override { return _key; }

    qh::schema::ToolResult execute(const qh::schema::ToolCall& call) override {
        if (_throw) throw std::runtime_error("probe boom");
        int cur = ++active();
        int p = peak().load();
        while (cur > p && !peak().compare_exchange_weak(p, cur)) {}
        if (_sleepMs > 0) std::this_thread::sleep_for(std::chrono::milliseconds(_sleepMs));
        --active();
        qh::schema::ToolResult r;
        r._toolCallId = call._id;
        r._output = "probe-" + call._id;
        r._isError = false;
        return r;
    }
    static void resetProbe() { active() = 0; peak() = 0; }
    static int peakValue() { return peak().load(); }
private:
    static std::atomic<int>& active() { static std::atomic<int> a{0}; return a; }
    static std::atomic<int>& peak()   { static std::atomic<int> p{0}; return p; }
    static qh::schema::ToolDefinition makeDef(const std::string& name) {
        qh::schema::ToolDefinition d; d._name = name; d._description = "probe"; d._inputSchema = json::object();
        return d;
    }
    std::optional<std::string> _key;
    int _sleepMs;
    bool _throw;
};

// 读改写计数工具（非原子，模拟"文件读改写"）：仅当被串行执行时 counter 才正确累加
class RacyCounterTool : public qh::tool::Tool {
public:
    RacyCounterTool() : Tool(makeDef()) {}
    std::optional<std::string> resourceKey(const qh::schema::ToolCall&) const override { return "same-file"; }
    qh::schema::ToolResult execute(const qh::schema::ToolCall& call) override {
        int c = counter();                 // 读
        std::this_thread::sleep_for(std::chrono::milliseconds(20));  // 放大竞争窗口
        counter() = c + 1;                 // 写
        qh::schema::ToolResult r; r._toolCallId = call._id; r._output = "ok"; return r;
    }
    static int value() { return counter(); }
    static void reset() { counter() = 0; }
private:
    static int& counter() { static int c = 0; return c; }
    static qh::schema::ToolDefinition makeDef() {
        qh::schema::ToolDefinition d; d._name = "racy"; d._description = "racy"; d._inputSchema = json::object();
        return d;
    }
};

QH_TEST(executeAll_preserves_order) {
    qh::tool::ToolManager tm;
    ProbeTool a("a", std::nullopt, 0), b("b", std::nullopt, 0), c("c", std::nullopt, 0);
    tm.registerTool(a); tm.registerTool(b); tm.registerTool(c);
    std::vector<qh::schema::ToolCall> calls{makeCall("1","a"), makeCall("2","b"), makeCall("3","c")};
    auto rs = tm.executeAll(calls, 0);
    QH_CHECK_EQ(rs.size(), (size_t)3);
    QH_CHECK_EQ(rs[0]._toolCallId, std::string("1")); QH_CHECK_EQ(rs[0]._output, std::string("probe-1"));
    QH_CHECK_EQ(rs[1]._toolCallId, std::string("2"));
    QH_CHECK_EQ(rs[2]._toolCallId, std::string("3"));
}

QH_TEST(executeAll_empty_calls) {
    qh::tool::ToolManager tm;
    auto rs = tm.executeAll({}, 0);
    QH_CHECK_EQ(rs.size(), (size_t)0);
}

QH_TEST(executeAll_diff_keys_run_concurrently) {
    qh::tool::ToolManager tm;
    ProbeTool a("a", "ka", 50), b("b", "kb", 50), c("c", "kc", 50);   // 不同 key，各 sleep 50ms
    tm.registerTool(a); tm.registerTool(b); tm.registerTool(c);
    ProbeTool::resetProbe();
    auto t0 = std::chrono::steady_clock::now();
    auto rs = tm.executeAll({makeCall("1","a"),makeCall("2","b"),makeCall("3","c")}, 0);
    auto dt = std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::steady_clock::now()-t0).count();
    QH_CHECK_EQ(rs.size(), (size_t)3);
    QH_CHECK(ProbeTool::peakValue() >= 2);                 // 真并发（峰值>=2）
    QH_CHECK(dt < 120);                                    // 并发总耗时远小于串行 150ms
}

QH_TEST(executeAll_same_key_run_serially_no_lost_update) {
    qh::tool::ToolManager tm;
    RacyCounterTool r; tm.registerTool(r);                 // resourceKey 恒 "same-file"
    RacyCounterTool::reset();
    auto rs = tm.executeAll({makeCall("1","racy"),makeCall("2","racy"),makeCall("3","racy")}, 0);
    QH_CHECK_EQ(rs.size(), (size_t)3);
    QH_CHECK_EQ(RacyCounterTool::value(), 3);              // 串行→无 lost update，计数==3
}

QH_TEST(executeAll_throwing_tool_isolated_as_error) {
    qh::tool::ToolManager tm;
    ProbeTool boom("boom", std::nullopt, 0, true), ok("ok", std::nullopt, 0, false);
    tm.registerTool(boom); tm.registerTool(ok);
    auto rs = tm.executeAll({makeCall("1","boom"),makeCall("2","ok")}, 0);
    QH_CHECK_EQ(rs.size(), (size_t)2);
    QH_CHECK(rs[0]._isError);                              // 抛异常→isError，不炸进程
    QH_CHECK_EQ(rs[1]._toolCallId, std::string("2"));      // 其他工具正常
    QH_CHECK(!rs[1]._isError);
}

QH_TEST(executeAll_bash_bucket_serial) {
    qh::tool::ToolManager tm;
    ProbeTool b1("b1", "__bash__", 30), b2("b2", "__bash__", 30);  // 同 __bash__ 桶
    tm.registerTool(b1); tm.registerTool(b2);
    ProbeTool::resetProbe();
    tm.executeAll({makeCall("1","b1"),makeCall("2","b2")}, 0);
    QH_CHECK_EQ(ProbeTool::peakValue(), 1);                // bash 桶串行
}

QH_TEST(executeAll_max_concurrency_limits_active) {
    qh::tool::ToolManager tm;
    ProbeTool a("a","ka",40), b("b","kb",40), c("c","kc",40), d("d","kd",40), e("e","ke",40);
    for (auto* t : {&a,&b,&c,&d,&e}) tm.registerTool(*t);
    ProbeTool::resetProbe();
    tm.executeAll({makeCall("1","a"),makeCall("2","b"),makeCall("3","c"),makeCall("4","d"),makeCall("5","e")}, 2);
    QH_CHECK(ProbeTool::peakValue() <= 2);                 // 限流：同时活跃<=2
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cmake --build build --config Release --target qharness_core_tests 2>&1 | grep -i error`
Expected: `executeAll` 不是 `ToolManager` 成员。

- [ ] **Step 3: 实现**（`ToolManager.h` 加 include + 声明）

```cpp
#include <future>      // 新增
#include <map>         // 新增
// ...
// 按 maxConcurrency 并发执行 calls：同 resourceKey 的调用串行、异 key 并发；
// 结果按 calls 原序返回。maxConcurrency<=0 表示无上限。任何工具抛异常都被隔离为 isError。
std::vector<schema::ToolResult> executeAll(const std::vector<schema::ToolCall>& calls,
                                           int maxConcurrency);
```

`ToolManager.cpp` 加实现（顶部 include `<future>`/`<map>`/`<algorithm>`）：

```cpp
std::vector<schema::ToolResult> ToolManager::executeAll(
    const std::vector<schema::ToolCall>& calls, int maxConcurrency) {
    const size_t n = calls.size();
    std::vector<schema::ToolResult> results(n);
    if (n == 0) return results;

    // 1) 分桶：key → 原索引列表（保持原序）。无工具/无 key → 唯一哨兵桶（自由并发）
    std::map<std::string, std::vector<size_t>> buckets;
    for (size_t i = 0; i < n; ++i) {
        Tool* tool = getTool(calls[i]._name);
        std::optional<std::string> key = tool ? tool->resourceKey(calls[i]) : std::nullopt;
        std::string bk = key.value_or("__free_" + std::to_string(i) + "__");
        buckets[bk].push_back(i);
    }

    // 2) 异常隔离：子线程 execute 万一抛 C++ 异常，转为 isError，绝不炸进程
    auto runOne = [this](const schema::ToolCall& c) -> schema::ToolResult {
        try {
            return this->execute(c);
        } catch (const std::exception& e) {
            schema::ToolResult r; r._toolCallId = c._id;
            r._isError = true; r._output = std::string("工具执行异常: ") + e.what();
            return r;
        } catch (...) {
            schema::ToolResult r; r._toolCallId = c._id;
            r._isError = true; r._output = "工具执行异常: 未知错误";
            return r;
        }
    };

    // 3) 每桶一个 async，桶内按原序串行 execute；桶间并发
    auto runBucket = [&](const std::vector<size_t>& idxs) {
        for (size_t idx : idxs) results[idx] = runOne(calls[idx]);
    };
    std::vector<std::pair<std::string, std::vector<size_t>>> bucketList(buckets.begin(), buckets.end());

    auto launchBatch = [&](size_t begin, size_t end) {
        std::vector<std::future<void>> fs;
        for (size_t b = begin; b < end; ++b)
            fs.push_back(std::async(std::launch::async, runBucket, std::cref(bucketList[b].second)));
        for (auto& f : fs) f.get();
    };

    if (maxConcurrency <= 0) {
        launchBatch(0, bucketList.size());
    } else {
        for (size_t b = 0; b < bucketList.size(); b += maxConcurrency)
            launchBatch(b, std::min(b + maxConcurrency, bucketList.size()));
    }
    return results;
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `./run_tests.bat`
Expected: 全部 `executeAll_*` 用例 PASS；`executeAll_same_key_run_serially_no_lost_update` 的 `value()==3`（证明同文件串行防 lost update）。

- [ ] **Step 5: Commit**

```bash
git add core/tool/ToolManager.h core/tool/ToolManager.cpp core/tests/test_ToolManager.cpp
git commit -m "feat(tool): ToolManager::executeAll 资源键分桶 Fork-Join 并发分发"
```

---

## Task 5: Engine 基类 setter + 两引擎分发循环改造

**Files:**
- Modify: `core/engine/Engine.h`（加成员 + setter）
- Modify: `core/engine/Engine2StageReAct.cpp:71-85`
- Modify: `core/engine/EngineReActLoop.cpp:46-61`
- Test: 现有 `test_Engine2StageReAct.cpp` / `test_EngineReActLoop.cpp`（回归）

**Interfaces:**
- Consumes: `ToolManager::executeAll`（Task 4）。
- Produces: `Engine::setMaxToolConcurrency(int)`、`Engine::_maxToolConcurrency`（默认 0）。

- [ ] **Step 1: 写回归验证**（确认现有引擎测试仍通过——单工具调用场景行为不变）

Run: `build/out/Release/qharness_core_tests.exe 2>&1 | grep -E "enginereactloop|engine2stage"`
Expected: 实现前先记录现有用例名（`enginereactloop_runs_react_cycle_with_mock`、`engine2stage_thinking_on_runs_two_phases`、`engine2stage_thinking_off_behaves_like_react`）。

- [ ] **Step 2: Engine 基类加成员 + setter**（`Engine.h`，`public:` 区 `run` 声明后加 setter；`protected:` 区 `_workDir` 后加成员）

```cpp
    virtual void run(const std::string& userPrompt) = 0;

    // 工具并发上限（0=无上限）；由 app 从 Settings 注入。executeAll 据此限流。
    void setMaxToolConcurrency(int n) { _maxToolConcurrency = n; }

protected:
    provider::Provider* _provider = nullptr;
    tool::ToolManager* _toolManager = nullptr;
    std::string _workDir;
    int _maxToolConcurrency = 0;
```

- [ ] **Step 3: 改造 `Engine2StageReAct.cpp`**（替换第 70-85 行"执行工具 + 观察"整段）

原代码：
```cpp
        info("模型请求调用 " + std::to_string(actionResult.message._toolCalls.size()) + " 个工具...");
        for (const auto& call : actionResult.message._toolCalls) {
            info("  -> 执行工具: " + call._name + ", 参数: " + call._arguments);
            schema::ToolResult toolResult = _toolManager->execute(call);
            if (toolResult._isError) {
                error("  -> 工具执行报错: " + toolResult._output);
            } else {
                info("  -> 工具执行成功 (返回 " + std::to_string(toolResult._output.size()) + " 字节)");
            }
            schema::Message observation;
            observation._role = schema::Role::User;
            observation._content = toolResult._output;
            observation._toolCallId = call._id;
            history.push_back(observation);
        }
```

替换为：
```cpp
        const auto& calls = actionResult.message._toolCalls;
        info("模型请求调用 " + std::to_string(calls.size()) + " 个工具，并发度="
             + (_maxToolConcurrency <= 0 ? std::string("无上限") : std::to_string(_maxToolConcurrency)));
        std::vector<schema::ToolResult> toolResults = _toolManager->executeAll(calls, _maxToolConcurrency);
        for (size_t i = 0; i < calls.size(); ++i) {
            const auto& tr = toolResults[i];
            if (tr._isError) error("  -> 工具[" + calls[i]._name + "] 报错: " + tr._output);
            else info("  -> 工具[" + calls[i]._name + "] 成功 (返回 "
                      + std::to_string(tr._output.size()) + " 字节)");
            schema::Message observation;
            observation._role = schema::Role::User;
            observation._content = tr._output;
            observation._toolCallId = calls[i]._id;
            history.push_back(observation);
        }
```

- [ ] **Step 4: `EngineReActLoop.cpp` 做相同替换**（第 46-61 行，`result.message._toolCalls` 对应替换为 `calls` + `executeAll` + 按序回填，逻辑同 Step 3）。

- [ ] **Step 5: 跑回归确认通过**

Run: `./run_tests.bat`
Expected: `enginereactloop_*`、`engine2stage_*` 全部 PASS（单工具场景行为不变），全量 `0 failures`。

- [ ] **Step 6: Commit**

```bash
git add core/engine/Engine.h core/engine/Engine2StageReAct.cpp core/engine/EngineReActLoop.cpp
git commit -m "feat(engine): 两引擎工具分发换用 executeAll 并发，并发度由基类 setter 注入"
```

---

## Task 6: EngineThread 注入 + SettingsDialog UI

**Files:**
- Modify: `app/EngineThread.cpp`（构造引擎后注入）
- Modify: `app/SettingsDialog.h`（加 `_concurrencySpin` 成员）
- Modify: `app/SettingsDialog.cpp`（引擎 Tab 加 QSpinBox + 收集/回显）

**Interfaces:**
- Consumes: `Settings::_maxToolConcurrency`（Task 1）、`Engine::setMaxToolConcurrency`（Task 5）。

- [ ] **Step 1: EngineThread 注入**（`EngineThread.cpp`，第 87 行 `_engine->setPostMessage(_postMessage);` 后加）

```cpp
    _engine->setPostMessage(_postMessage);
    _engine->setMaxToolConcurrency(_settings._maxToolConcurrency);
```

- [ ] **Step 2: SettingsDialog 加成员**（`SettingsDialog.h`，`class QRadioButton;` 前向声明区加 `class QSpinBox;`；`private:` 成员区 `_reactRadio` 旁加 `QSpinBox* _concurrencySpin = nullptr;`）

- [ ] **Step 3: SettingsDialog 引擎 Tab 加控件**（`SettingsDialog.cpp`，引擎 Tab 构造区 `_twoStageRadio` 之后、`engV->addStretch()` 之前加）

```cpp
    engV->addStretch();
    _tabs->addTab(eng, "引擎");
```
改为在 `addStretch` 前插入并发度行：

```cpp
    engV->addWidget(new QLabel("工具并发上限（0=无上限）："));
    _concurrencySpin = new QSpinBox;
    _concurrencySpin->setRange(0, 32);
    _concurrencySpin->setSpecialValueText("0（无上限）");   // 0 显示为"无上限"
    _concurrencySpin->setValue(0);
    engV->addWidget(_concurrencySpin);
    engV->addStretch();
    _tabs->addTab(eng, "引擎");
```
（`SettingsDialog.cpp` 顶部 include 区确认 `#include <QSpinBox>`。）

- [ ] **Step 4: SettingsDialog 回显/收集**（`setSettings` 末尾 `_loading=false;` 前加；`getSettings` `return s;` 前加）

`setSettings`：
```cpp
    _concurrencySpin->setValue(s._maxToolConcurrency);
    _loading = false;
```
`getSettings`：
```cpp
    s._maxToolConcurrency = _concurrencySpin->value();
    return s;
```

- [ ] **Step 5: 构建验证 + 手动端到端**

Run: `./build_msvc.bat`
Expected: 构建成功，`build/out/Release/qharness_app.exe` 运行，设置对话框引擎 Tab 可见"工具并发上限"QSpinBox，默认显示"0（无上限）"；改为 4 后确定、重开设置回显 4（持久化到 `config/setting.json` 的 `maxToolConcurrency` 字段）。

Run: `./run_tests.bat`
Expected: `0 failures`（Settings 序列化已由 Task 1 覆盖；UI 为手动验证）。

- [ ] **Step 6: Commit**

```bash
git add app/EngineThread.cpp app/SettingsDialog.h app/SettingsDialog.cpp
git commit -m "feat(app): 设置对话框增加工具并发上限，EngineThread 注入到引擎"
```

---

## Self-Review

**1. Spec 覆盖**：对照设计 §8 改动清单——Settings（T1）、Tool::resourceKey 基类（T2）、文件类 override（T2）、Bash 系列 override（T3）、ToolManager::executeAll（T4）、Engine setter（T5）、两引擎改造（T5）、EngineThread 注入（T6）、SettingsDialog UI（T6）、test_ToolManager executeAll 用例（T4）、test_Settings 序列化用例（T1）。设计 §9 测试计划全部落到 T4 用例（顺序/并发加速/lost update/bash 串行/限流/异常隔离/空）。无遗漏。

**2. Placeholder 扫描**：Task 4 Step 1 的"占位删除"说明已显式标注并给出最终用例正文；其余步骤均含完整代码/命令。无 TBD/TODO。

**3. 类型一致性**：`_maxToolConcurrency`（int，0=无上限）在 T1/T5/T6 一致；`resourceKey` 返回 `std::optional<std::string>` 在 T2/T3/T4 一致；`executeAll(calls, maxConcurrency) -> vector<ToolResult>` 在 T4/T5 一致；`setMaxToolConcurrency(int)` 在 T5/T6 一致。`__bash__` 键在 T3/T4 一致。

---

## Execution Handoff

Plan complete and saved to `docs/plans/2026-07-01-工具分发Fork-Join重构实现计划.md`. Two execution options:

**1. Subagent-Driven（推荐）** — 每个 task 派发独立 subagent 实现，task 间两阶段 review，快速迭代。

**2. Inline Execution** — 本会话内用 executing-plans 批量执行，带检查点 review。

Which approach?
